# HuPa Physical AI Daily

**This file is no longer the source of truth.**

As of 2026-05-21 the daily briefings live in `days/YYYY-MM-DD.md` (one file per day, rendered to `days/YYYY-MM-DD.html`), and `index.html` is auto-synced to the latest day's HTML. Monthly reviews live in `monthly/YYYY-MM.md` and are written on the 1st of each month.

See `README.md` for the new layout, or run `./build.sh` after editing any day or monthly file to regenerate the site.
